<?xml version="1.0" encoding="utf-8"?>
<!-- Selector de color para el texto de la fecha -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Color cuando el texto está habilitado -->
    <item android:color="@color/gray_dark" android:state_enabled="true" />
    <!-- Color por defecto para otros estados -->
    <item android:color="@color/gray_light" />
</selector>